site_name: Python Boilerplate
theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [.]

nav:
  - Home: index.md
  - API:
      - Boilerplate: api/boilerplate.md